# events {
#     worker_connections 1024;
# }

# http {
#     upstream flask_backend {
#         # Docker's built-in DNS resolves service name to all container IPs
#         server app:5000;
#     }

#     server {
#         listen 80;
        
#         location / {
#             proxy_pass http://flask_backend;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         }
#     }
# }


events {
    worker_connections 1024;
}

http {
    # Docker's internal DNS server with short TTL for quick updates
    resolver 127.0.0.11 valid=5s ipv6=off;
    
    # Access log
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    server {
        listen 80;
        
        location / {
            # Use variable to force runtime DNS resolution
            set $backend "app";
            proxy_pass http://$backend:5000;
            
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Short timeouts to fail fast and retry
            proxy_connect_timeout 2s;
            proxy_send_timeout 5s;
            proxy_read_timeout 5s;
            
            # Retry on next upstream on failure
            proxy_next_upstream error timeout http_502 http_503 http_504;
        }

        location /health {
            set $backend "flask-app";
            proxy_pass http://$backend:5000/health;
            proxy_set_header Host $host;
        }
        
        # Nginx status endpoint
        location /nginx_status {
            stub_status on;
            access_log off;
        }
    }
}